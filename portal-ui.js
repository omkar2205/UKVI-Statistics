function showSection(id){document.querySelectorAll('.section').forEach(s=>s.classList.toggle('active',s.id==id));document.querySelectorAll('.side-link,.top-tab').forEach(b=>b.classList.toggle('active',b.dataset.target==id));setTimeout(()=>Object.values(CH).forEach(c=>c&&c.resize()),80)}
function toggleCountryMenu(ev){ev.stopPropagation();countryMenu.classList.toggle('open')}function closeCountryMenu(){countryMenu.classList.remove('open')}function renderCountryMenu(){let t=cl(countrySearch?.value||'').toLowerCase(),v=N.filter(x=>x.toLowerCase().includes(t));countryList.innerHTML=v.map(c=>`<label class="country-option"><input type="checkbox" value="${e(c)}" ${SEL.includes(c)?'checked':''} onchange="toggleCountry(this)"><span>${e(c)}</span></label>`).join('');countryLabel.textContent=!SEL.length?'All countries':SEL.length==1?SEL[0]+' selected':SEL.length+' countries selected'}function toggleCountry(i){i.checked?SEL.push(i.value):SEL=SEL.filter(x=>x!=i.value);SEL=u(SEL).sort();renderCountryMenu()}function selectVisibleCountries(){document.querySelectorAll('#countryList input').forEach(i=>{if(!SEL.includes(i.value))SEL.push(i.value)});SEL=u(SEL).sort();renderCountryMenu()}function clearCountries(){SEL=[];renderCountryMenu()}
function oc(v){v=cl(v).toLowerCase();return v.includes('issued')||v.includes('grant')?'issued':v.includes('refused')||v.includes('refusal')||v.includes('rejected')?'refused':null}function cl(v){return String(v??'').replace(/\s+/g,' ').trim()}function num(v){let n=Number(String(v??'').replace(/,/g,''));return isFinite(n)?n:0}function qt(v){let m=cl(v).toUpperCase().match(/\bQ([1-4])\b/);return m?'Q'+m[1]:cl(v)}function yr(y,q){let n=num(y);if(n>1900)return n;let m=cl(q).match(/\b(20\d{2})\b/);return m?+m[1]:0}function u(a){return[...new Set(a.filter(x=>x!==''&&x!=null))]}function sum(a,k){return a.reduce((t,x)=>t+(+x[k]||0),0)}function top(a,k,v){return group(a,k,v).sort((a,b)=>b.value-a.value)[0]}function topRows(a,k,v){return group(a,k,v).sort((a,b)=>b.value-a.value).slice(0,15)}function group(a,k,v){let m={};a.forEach(x=>{let l=k=='y'?String(x.y):x[k];m[l]=m[l]||{label:l,value:0};m[l].value+=+x[v]||0});return Object.values(m)}function rate(n,d){return d?n/d:0}function chg(c,p){return p?(c-p)/p:null}function rd(v){return Math.round((+v||0)*10)/10}function fmt(v){return Math.round(+v||0).toLocaleString('en-GB')}function pct(v){return rd((+v||0)*100).toFixed(1)+'%'}function spct(v){if(v==null)return'-';let n=rd(v*100);return(n>0?'+':'')+n.toFixed(1)+'%'}function spp(v){if(v==null)return'-';let n=rd(v*100);return(n>0?'+':'')+n.toFixed(1)+' pp'}function e(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function set(id,v){let x=document.getElementById(id);if(x)x.textContent=v}function err(m){errorBox.style.display='block';errorBox.textContent='Error: '+m}function csv(n,r){let b=new Blob([r.map(x=>x.join(',')).join('\n')],{type:'text/csv'}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=n;a.click();URL.revokeObjectURL(u)}
